# 已知问题

记录当前已知问题、成因和规避方式，避免后续重复排查。

---

## 一、cannon-es 静态刚体包围盒不更新

**现象**：车辆在高台上完全无法驱动，四个轮子全部检测不到地面，车身直接坐在地台表面；同时车损与重置都正常。

**成因**：cannon-es 新建刚体的 `aabb` 是局部坐标，且 `aabbNeedsUpdate` 为 false。物理世界在做宽相查询和射线检测时会用这个包围盒做快速剔除，于是远处的静态碰撞体被整体过滤掉，悬挂射线打不中地面。

地面之所以一直正常，是因为地面尺寸是 600 米见方，局部包围盒恰好覆盖了车辆所在的坐标区间，属于侥幸。

**规避**：`src/physics/Helpers.js` 中所有刚体在加入世界后立刻调用 `body.updateAABB()`；地图里手动创建的刚体同样处理；重置重物块位置后也重新刷新。

**影响范围**：新增静态碰撞体时若忘记刷新包围盒，就会复现。新增时务必走 Helpers 里的创建函数。

---

## 二、旋转后的 Plane 不能做地面

**现象**：车辆前轮永远悬空，后轮着地，车头向上翘，踩油门几乎不动，只会缓慢后退。

**成因**：cannon-es 的 Plane 在计算世界包围盒时不考虑刚体旋转，旋转 90 度以后得到的包围盒仍然是局部朝向，导致车辆前半部分的射线被过滤。

**规避**：地面改用大尺寸 Box，顶面放在 y 等于 0 的位置。

---

## 三、高空坠落伤害偏低

**现象**：从三十米高台坠落，车损只有百分之几，看起来几乎没摔坏。

**成因**：悬挂会吸收绝大部分垂直冲击，车身碰撞体根本接触不到地面，也就没有碰撞事件，自然没有伤害。

**规避**：`Vehicle.checkHardLanding` 用垂直速度骤减来判断硬着陆，落地瞬间按下降速度补算一次撞击。阈值九米每秒。

---

## 四、车身变形只影响视觉

车身凹陷是顶点变形，不动物理碰撞体。这是刻意取舍。若每次变形都重建碰撞网格，手机会直接掉帧。所以撞凹以后车的碰撞轮廓仍是原始形状。

---

## 五、零件脱落后数量上限

场景内脱落零件和掉落轮胎最多同时存在二十六个，超出后最早的一个自动归位消失。障碍物本身最多十七个可撞飞，加上地图静态体，活动刚体总量控制在四十以内。

---

## 六、测试环境帧率说明

自动化测试跑在无显卡的软件渲染环境，帧率约二十，导致物理时间比真实时间慢，测试脚本里的等待时间需要给得比直觉更长。这不是游戏本身的问题，真机有 GPU 加速。

---

## 七、跳台造型偏扁平

三个跳台的视觉造型是倾斜平板加底座，能正常起飞和翻滚，但外观不够像专业跳台。属于表现问题，不影响玩法。

---

## 八、星星小车的版权边界（已处理）

早期版本这辆车叫星宝风格小车，造型是粉色圆润卡通加一双大眼睛、腮红和笑脸。即便代码与素材都是原创的，只把名字改掉并不能真正规避风险，因为仍然在复现某个既有角色的形象特征。

公开发布前已做完整原创化处理，改动如下。

- 命名改为星星小车，英文 Star car
- 移除全部拟人面孔元素，没有眼睛、腮红、嘴
- 造型母题改为五角星，包含星形大灯、星形尾灯、星形侧贴花、星形轮毂、车顶星标、星形尾翼
- 车顶加独立深色圆顶与前格栅，造型向通用小跑车靠拢
- 增加可破碎的挡风玻璃与后窗，与普通轿车一致

现在这辆车与任何第三方角色都不存在视觉关联，也不再需要额外的使用限制。相关记录见 THIRD_PARTY_ASSETS.md。

---

## 九、退出按钮在浏览器里无效

网页受浏览器限制，无法真正关闭页面。点击退出会回到主菜单并停掉引擎声。手机上建议直接切走或关掉标签页。

---

## 十、画质切换需要重进

切换画质后像素比、阴影开关与阴影分辨率会立即生效，但部分材质与阴影贴图需要重新进入一次试验场才能完全应用。界面会给出提示。

---

## 十一、Debug 包不能上架

当前交付的 CrashLab-debug.apk 使用 Debug 签名，仅用于真机安装测试。要上架或对外正式分发，需要在 android 目录配置自己的签名 keystore 后构建 Release 包。

---

## 十二、Android 工具链版本要求

Capacitor 8 依赖的 androidx 库要求 compileSdk 至少 36，用 compileSdk 34 会在 checkDebugAarMetadata 阶段直接失败。当前配置为 compileSdk 36 加 targetSdk 34，前者满足依赖，后者让运行时行为停在 Android 14 语义，避免新系统的强制变更带来意外。

本机已安装的 SDK 组件：platforms android-34 与 android-36、build-tools 34.0.0 与 35.0.0、platform-tools。Gradle 需要 platform 36 时若网络不稳，会卡在 Preparing Install Android SDK Platform 36 且长时间无输出，此时手动用 sdkmanager 装一次 platforms;android-36 即可，装完再构建就顺畅了。

---

## 十三、星星小车的玻璃破碎

早期版本这辆车没有车窗，大眼睛曾被误当成玻璃在撞击时替换成碎玻璃，修复方式是让眼睛永不破碎。

原创化以后的星星小车已有真正的挡风玻璃与后窗，参与标准玻璃破碎逻辑，与普通测试轿车一致。车身不再有眼睛，这条历史遗留问题自然消失。

---

## 十四、返回键行为依赖原生壳

返回键优先回主菜单、主菜单再按才退出，这套行为只在 APK 里生效，浏览器里返回键由浏览器接管。网页调试时请用 Esc 回菜单。

---

## 十五、APK 未在真机上跑过

Debug APK 已构建成功并通过包体与元数据核验，离线可用性用真实浏览器在断网状态下验证过。但本轮没有 Android 真机或模拟器，触屏手感、实际帧率、沉浸式全屏效果、返回键与切后台暂停需要装到真机上再确认一遍。

---

## 十六、轮胎是血量制不是碰撞即掉

四轮各有独立血量，血量归零才脱落。撞击点一点六米内的轮子按距离扣血，血量越低抓地力越低、轮胎视觉越扁越黑。一次重撞只会把轮胎打伤，反复撞同一个位置才会打掉。

轮子脱落后再修车会完整归位。因为轮子网格同时也是掉落刚体的网格，归位依赖 reset 时的删减与重挂顺序，改这块代码时要注意先清掉落物再还原轮子。

---

## 十七、新区块把绘制批次推到两百左右

加了保龄球道十个球瓶与砖墙阵三十六块砖以后，绘制批次从原来的一百三十多升到两百到两百二十，三角形五千五到一万零五百。

这些碎块每个都是独立网格，没办法简单合批，因为撞飞以后要各自运动。普通手机在这个量级仍然流畅，但如果后续还要继续加可撞飞的碎块，需要先换成实例化渲染，否则批次会继续线性上涨。

---

## 十八、下压力施力点是易错点

cannon-es 的 `applyForce(force, relativePoint)` 第二个参数是相对刚体中心的偏移量，不是世界坐标。早期版本传了世界坐标，等效于给车体接了一根随位置变长的力臂，平坦路面直线行驶也会被掀翻。

任何给刚体施加力或冲量的地方都要注意这一点，需要绕某点施力时传的是相对偏移。详见 DEVLOG 第四轮。

---

## 十九、坡道的三个坑，一个都不能少

跳台区的坡前后修了三轮才彻底通。三个条件是绑在一起的：

1. 绕 X 轴必须取负角，让迎车那侧低。取正角的话车正面撞上的是坡背面的竖直端面。
2. 高度必须以坡的顶面为基准算，让前缘几乎贴地。公式是 `centerY = gap - halfThick * cos + (length / 2) * sin`，**中间那个符号极易写错**，写成减号就会把整块坡压到地面以下，只留一条两厘米的边露出来，场地看上去就是一片平地。校验方法：低端顶面应等于 gap，高端顶面应等于 gap 加 length 乘 sin。
3. 坡体本身要做厚，一路埋到地面以下，**不能另外加矩形支撑座**。支撑座如果比坡面高，坡上就会立起一道横挡，车头直接撞进支撑座里。

新增坡道时直接调用 Arena 的 buildRamp，不要自己拼方块。

---

## 二十二、验证坡道必须同时看通过性与起飞高度

有个惨痛教训。坡被埋进地下那一次，验证脚本只检查了车能不能开过去，结果车在平地上顺顺当当开过去，判定为通过，问题被完全掩盖，直到用户反馈场地里看不到飞坡台才发现。

现在验证条件是两个：车能开过去，而且最高点要超过一点六米。健全的坡应该让普通车飞到五米以上。

任何涉及跳跃地形的验证都要用高度指标，不能用能否通过。

---

## 二十、水池的池边不能做高

第一版水池四周做了 0.7 米高的围条，车冲进去就被挡住，出不来。现在围条只有 0.12 米，车能直接碾进去也能开出来，实测能以 195 公里每小时穿池而出。

任何要让车自由进出的地形边界都要低于轮子半径，普通车是 0.36 米。

---

## 二十三、车损显示与爆炸判定要共用同一个阈值

界面上的车损用四舍五入显示，血量掉到百分之二以下就已经显示 100%。爆炸判定如果写成血量小于等于零加一个小量，中间就会有一段显示满车损却不爆炸的区间。

判定阈值统一取 0.02，与显示对齐。

---

## 二十四、圆环赛道里必须换贴身相机

三百六十度垂直圆环内部，跟随视角的相机按世界坐标落在车后八米，这个位置在圆环外面，视线被圆环壁整个切断，屏幕上什么都看不到。

判定方式与水池一样，Arena 暴露 loopZone，车辆每帧标记 inLoop，相机在这个区域内改成用车身局部偏移定位的贴身机位。

任何封闭管道或环状地形都要考虑相机遮挡，不要指望默认跟随视角能穿过结构。

---

## 二十五、家长控制说明

密码固定为 456321，写在 src/core/ParentalControl.js 的 INITIAL_PIN 里，故意不提供修改入口，避免被孩子改掉。

每次打开应用都要重新输入，授权状态只存在内存里，不落盘，所以关闭重开一定重来一遍。

游玩时长设置会存到 localStorage，键名 crashlab.parental.v1。

如果家长忘了密码，只能改代码或清除应用数据，这一点在 README 里也写明了。



