# 参与贡献

感谢愿意花时间。这个项目规模不大，先把约定说清楚，改起来会顺很多。

---

## 先跑起来

需要 Node 18 以上。安卓相关改动还需要 JDK 21 与 Android SDK。

```bash
npm install
npm run dev
```

浏览器打开终端打印的地址。开发调试建议用桌面浏览器，改代码有热更新。

改完必须确认构建通过：

```bash
npm run build
```

---

## 代码约定

### 数值改动只动 CarSpecs

六辆车的质量、悬挂、轮胎、下压力、特技参数全部在 `src/vehicle/CarSpecs.js`。调车感请只改这个文件，不要把数值散落到别处。

判断车感是否变好，别靠看截图。用变形顶点质心、车损百分比、起飞高度这类客观指标，跑 `test/` 下的脚本，把 `docs/verify-*.json` 里的前后数字摆出来。

### 车辆物理共用一套逻辑

六辆车共用 Vehicle.js 的控制、伤害、重置代码，差异只走参数。如果为新车型加特判分支，先在 Issue 里说明为什么参数化解决不了。

### 地形高程改动要连带改地面块

`src/world/Arena.js` 里山地高程函数与地面块尺寸是耦合的：块要小于波形最短波长，块与块之间要有重叠，块中心高程取场地坐标而不是局部坐标。只改一处会立刻出现台阶，车会卡在上面。

### 不要引入外部素材

模型、贴图、音频一律程序化生成。提交任何二进制素材之前先开 Issue 讨论，并同时更新 THIRD_PARTY_ASSETS.md。

### 界面文案

面向孩子，按钮文字短，用词直白，不出现感叹号和营销腔。

---

## 提交前自检

- [ ] `npm run build` 通过，零报错
- [ ] 改动涉及玩法时，跑了对应的 `test/verify-*.cjs` 脚本
- [ ] 没有把 `node_modules`、`dist`、`android/app/build`、`local.properties` 加进版本库
- [ ] 没有提交任何密钥、令牌、签名文件
- [ ] 涉及数值取舍时，把验证数据写进 `docs/` 并在描述里引用

---

## 提交信息

一句话说清做了什么，必要时补一句原因。例如：

```
山林地形改为连续起伏路面
地块尺寸与高程波长不匹配，交界处有 0.7 米错台，车会卡住
```

---

## 反馈问题

开 Issue 时请带上以下信息，能省一轮来回：

- 手机型号与 Android 版本，或者浏览器与版本
- 哪辆车、哪个出生点、什么操作
- 期望的结果与实际的结果
- 截图或录屏，崩溃的话附 logcat

---

## 许可证

提交代码即表示同意以本项目的 MIT 许可证发布你的贡献。
